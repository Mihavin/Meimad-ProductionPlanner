<UserControl x:Class="Meimad.Planner.Client.Windows.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="620">
    <Grid Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="#D8DDE3"
                BorderThickness="1" CornerRadius="5" Padding="18">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="From (UTC)" FontWeight="SemiBold" />
                        <DatePicker SelectedDate="{Binding FromDate}" Width="145" Margin="0,5,0,0" />
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="To (UTC, exclusive)" FontWeight="SemiBold" />
                        <DatePicker SelectedDate="{Binding ToDate}" Width="145" Margin="0,5,0,0" />
                    </StackPanel>
                    <Button Content="Calculate / Refresh" Command="{Binding RefreshCommand}" Margin="0" />
                </StackPanel>
                <StackPanel Margin="0,0,24,0">
                    <TextBlock Text="TIMELINE" FontSize="20" FontWeight="Bold" />
                    <TextBlock Text="Read-only rendering of the Server calculation. This screen cannot schedule or rearrange work."
                               Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding StatusMessage}" Margin="0,9,0,0" FontWeight="SemiBold" TextWrapping="Wrap" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#D8DDE3" BorderThickness="1"
                CornerRadius="5" Padding="14" Margin="0,12,0,0">
            <WrapPanel>
                <TextBlock Text="LEGEND" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0" />
                <StackPanel Orientation="Horizontal" Margin="0,0,18,0"><Border Width="16" Height="16" Background="#FBC02D" /><TextBlock Text="SETUP" Margin="6,0,0,0" /></StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,18,0"><Border Width="16" Height="16" Background="#1E88E5" /><TextBlock Text="PRODUCTION" Margin="6,0,0,0" /></StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,18,0"><Border Width="16" Height="16" Background="#9E9E9E" /><TextBlock Text="IDLE" Margin="6,0,0,0" /></StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,18,0"><Border Width="16" Height="16" Background="#C62828" /><TextBlock Text="DOWNTIME" Margin="6,0,0,0" /></StackPanel>
                <StackPanel Orientation="Horizontal"><Border Width="16" Height="16" Background="#F57C00" /><TextBlock Text="RESERVED" Margin="6,0,0,0" /></StackPanel>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#D8DDE3" BorderThickness="1"
                CornerRadius="5" Padding="12" Margin="0,12,0,0">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="440">
                <Canvas x:Name="TimelineCanvas" MinHeight="120" />
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="#D8DDE3" BorderThickness="1"
                    CornerRadius="5" Padding="16">
                <StackPanel>
                    <TextBlock Text="CONFLICTS" FontWeight="Bold" FontSize="16" />
                    <TextBlock Text="Each status includes a severity label; color is supplemental."
                               Foreground="{StaticResource MutedTextBrush}" Margin="0,3,0,10" />
                    <ItemsControl ItemsSource="{Binding Conflicts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="4,0,0,0"
                                        Background="#FAFAFA" Padding="10" Margin="0,0,0,8">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="{StaticResource YellowBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Severity}" Value="blocking"><Setter Property="BorderBrush" Value="{StaticResource RedBrush}" /></DataTrigger>
                                                <DataTrigger Binding="{Binding Severity}" Value="warning"><Setter Property="BorderBrush" Value="{StaticResource OrangeBrush}" /></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SeverityLabel}" FontWeight="Bold" />
                                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,3,0,0" />
                                        <TextBlock Text="{Binding Code}" Foreground="{StaticResource MutedTextBrush}" FontFamily="Consolas" FontSize="11" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#D8DDE3" BorderThickness="1"
                    CornerRadius="5" Padding="16">
                <StackPanel>
                    <TextBlock Text="SELECTED BATCH DEPENDENCIES" FontWeight="Bold" FontSize="16" />
                    <ComboBox ItemsSource="{Binding Batches}" SelectedItem="{Binding SelectedBatch}"
                              DisplayMemberPath="DisplayName" Margin="0,9,0,10" MinHeight="34" />
                    <TextBlock Text="Only dependency edges for the selected batch are shown."
                               Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,10" />
                    <ItemsControl ItemsSource="{Binding SelectedDependencies}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F3F5F7" BorderBrush="#D8DDE3" BorderThickness="1"
                                        CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding RuleLabel}" Foreground="#1E88E5" FontWeight="Bold" Margin="0,4,0,0" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
